rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Audio files are publicly readable
    match /audio/{audioId} {
      allow read: if true;
      allow write: if request.auth != null && 
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'editor';
    }
  }
}

